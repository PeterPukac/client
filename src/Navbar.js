import { Link } from "react-router-dom";
const Navbar = (props) => {
    return (
      <div className="container">
        <img src="./logo.PNG"></img>
      </div>

    );
  
}

export default Navbar;